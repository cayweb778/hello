```aidl
// --------- 渲染性能代码优化： 使组件加载或状态切换的逻辑不影响交互渲染性能 start ---------

// 1. 组件文件加载与组件使用不影响性能，组件逻辑影响性能

// - 组件文件加载（import ....）:                                    不影响性能
// - 组件使用 （<在页面<MainView></MainView>）:                       不影响性能
// - 组件逻辑： （MainView中的<template>与<script>与<style>）          影响性能

// import 引入组件
// <MainView></MainView> 使用组件
// <MainView></MainView>的上级控制是否加载【组件逻辑】v-if，【上级可以是div的v-if】,也可以是插槽组件中<slot v-if="showView"></slot>


// 2. 例子：
//   - 使路由视图逻辑不影响系统架构运行： 先把系统UI布局架子渲染后，再渲染路由视图
//   - 使组件加载或状态切换不影响交互性能
//   - ...

// 解决的问题:
//    - 组件直接交互卡顿
//    - 加载条卡顿
//    - 页面刷新卡顿：使路由视图逻辑不影响系统架构运行



// 3. 优化页面性能
// 流程：上级做一个插槽<ViewLayoutProvider>,onMounted()在渲染Layout后,加载自己的组件逻辑

// --------- 渲染性能代码优化： 使组件加载或状态切换的逻辑不影响交互渲染性能 end ---------
```